import 'package:intl/intl.dart';

class dataSelectedDay {
  static DateTime selectedDay = DateTime.now();

  // static String dateString =
  //     DateFormat('yyyy-MM-dd HH:mm:ss').format(selectedDay);
  // static String date = dateString.substring(0, 19);
  // // static DateTime selectedDay1=  DateTime.parse(date);
  // static DateTime selectedDayFormatted = DateTime.parse(date);
}
